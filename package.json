{
  "name": "kisan-setu",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix digital-krishi-frontend install && npm --prefix digital-krishi-frontend run build",
    "frontend:dev": "npm --prefix digital-krishi-frontend run dev",
    "backend:dev": "npm --prefix digital-krishi-backend start"
  }
}
